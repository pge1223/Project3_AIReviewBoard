# 종합 리포트 — 개선 실험 기록 (Before / After)

> 작성: 경이 / 2026-07-26 · 가이드북 9.5-④ "개선 실험 기록" 대응.
> 모든 수치는 이 레포에서 실측된 값이며, 각 실험의 재현 경로는 devlog(2026-07-23 ~ 07-26)에 있다.
> 발표 슬라이드에서 표 그대로 사용 가능.

## 실험 1 — 채점 재현성: temperature=0 + seed 고정

| | Before | After |
|---|---|---|
| 조건 | 같은 문서·같은 rubric 재채점 | 좌동 + `temperature=0` + `seed` 고정 |
| 총점 | **31.5 ↔ 64** (편차 ±32.5) | **54.5 / 56 / 55.5** (3회, 편차 ±1.5) |
| 해석 | 경계(애매한) 문서에서 LLM 판정이 flip → 점수가 운에 좌우 | 같은 문서 = 같은 점수에 근접 → 버전 추적(상승/하락)이 의미를 가짐 |

## 실험 2 — 변별력: 판정 밴드 캘리브레이션 + 엄정 채점 + 결정론 상한

| | Before | After |
|---|---|---|
| 조건 | 밴드 없음(LLM이 무난히 중상위권 부여) | judgment→배점 비율 밴드 + [엄정 채점] 강제 + 근거 신호 기반 점수 상한 |
| 근거 없는 문서 | **~82점** (잘 쓴 문서와 구분 불가) | **21.5점** (1차) / **55.5점** (2차 조건) |
| 잘 쓴 문서 | 82점 | **77~81점 유지** (엄격화에도 깎이지 않음 — 역검증) |
| 해석 | "다 80점대"라 피드백 루프 무의미 | 품질 차이가 점수 차이로 나타남 (데이터 항목 55% vs 90%) |

## 실험 3 — 기준 충실성: rubric 지어내기 3중 차단 (v5→v7)

실제 발생한 사고 3건과 차단 결과:

| 사고(실측) | 원인 | 차단 장치 | After |
|---|---|---|---|
| 잘못된 URL 스크랩(43자 잡문)인데 "기본 4범주 × 25점" rubric이 '추출 성공'으로 표시 | LLM이 프롬프트의 참고 목록을 복사·창작 | **사전 가드** — 평가/배점 신호 없으면 LLM 호출 자체 차단 | 정적 폴백 + UI 빨간 경고 "공고문 기준이 아님" |
| 신청서식(붙임2)의 작성 항목 제목 4개가 25점 균등 배분으로 rubric화 | "배점 없으면 균등 배분" 프롬프트 규칙 | 규칙 삭제 + **배점 숫자 원문 확인**(과반 미확인 시 추출 거부) | 거부 → 정직 폴백 (실측: unmatched 4/4 → 거부) |
| 부문별 배점 열 혼용 — 확산성·효과성이 우수사례 열(25) 채택, 만점 100 | "합은 반드시 100" 규칙 + 열 2개 표 | 규칙 삭제 + **원문 첫 부문 열 값으로 강제 정렬** | 25→10, 15→10 보정 → **만점 85 정확 재현** (15+30+30+10) |

## 실험 4 — 근거 기반성: 지적↔원문 문장 1:1 검증 (issue_refs)

| 입력(지적 속 인용) | 판별 결과 | 화면 표시 |
|---|---|---|
| 원문에 실제 있는 인용 `(p.2) '구체적인 수치 목표는 추후에 정할 예정'` | `verified: true` | STEP 1 "제출 문서 근거"로 표시 |
| 원문에 없는(지어낸) 인용 | `verified: false` | **표시되지 않음** |
| 인용이 없는 지적 | ref 없음 | 항목 단위 인용으로 폴백 |

- 부분 문장이 더 긴 인용에 포함되면 **가장 완전한 인용 1개만** 표시(중복 제거 일반 규칙 — 전 항목 적용).
- 인용 표시는 색인된 evidence의 **KURE 의미 유사도 score 내림차순** 상위 4개.

## 실험 5 — 주관성 배제: 측정 가능 항목만 채점

| | Before | After |
|---|---|---|
| 만점 | 항상 100 (주관 항목 포함) | **측정 가능 항목 배점 합** (NIA 실증·PoC = **85점**) |
| 안전성·윤리성 | 채점에 포함 | **제외 + 사유 표기** ("정답이 없는 가치 판단 영역…") |
| 가점 요소 | 혼입 위험 | 별도 분리(공모전마다 변동·증빙 필요 → 자동 채점 제외) |
| 사용자 고지 | 없음 | 상단 "총점은 참고용" 배너 + 점수 체계표(채점/제외/사유) |

## 회귀 안전망

- `ai/meeting/tests/` **366개 테스트 전부 통과** 유지 (동적 rubric 검증·배점 보정·버전 비교·calibration 포함)
- 계약(`review_output.schema.json`)은 선택 필드 추가만(v1.1 규칙): `extracted_from_notice` · `excluded_criteria` · `issue_refs`
